# CKB tx-pool authority decisions

This file owns only the nine user/project decisions that constrain G0. Source
facts, findings, progress and the next action belong to their machine owners.

## 九、当前主仓吸收状态

| ID | Constraint |
|---|---|
| `CKB-AUTH-0001` | Relay is item/byte/work/close bounded. `UnknownParents` and `GenerationReset` remain ordered boundaries; extreme pressure need not retain every detail. |
| `CKB-AUTH-0002` | The open architecture-class optimum remains research; delivery must not claim that unbounded theorem. |
| `CKB-AUTH-0003` | Do not revive SCC/SDD/SAA or enlarge unsafe reclamation without frozen causal performance evidence. |
| `CKB-AUTH-0004` | VM verification time is local, retryable, non-banning and non-consensus; it includes ELF load, excludes queue time and preserves CKB-VM semantics. |
| `CKB-AUTH-0005` | Correctness, liveness, boundedness and concurrency precede final develop performance ranking; causal guards may reject a local simplification but cannot establish the winner. |
| `CKB-AUTH-0006` | Legacy `BlockExt` database upgrade is outside tx-pool and must not create a tx-pool version scan, reverify or fallback. |
| `CKB-AUTH-0007` | Canonical delivery seeks the best result-before-frozen finite candidate, including reviewer cognition and change amplification; it does not close open-class research. |
| `CKB-AUTH-0008` | Ordinary independent work has no global serial fallback. Transactions sharing only a read-only cell-dep outpoint remain independent and require production-bound end-to-end overlap evidence. |
| `CKB-AUTH-0009` | Coupled effects become durable with their authority transition and publish only afterward. The current `EffectLog` is an incumbent, not a preapproved G0 shape; no early best-effort publication or second semantic journal is allowed. |

The long-running role is the **G0-accountable Primary engineering owner**. The
Primary selects roots, integrates code, adjudicates evidence, preserves cold
continuity and owns reviewer/maintenance cost. Machine state aids recovery; it
does not replace engineering judgment or turn Partner/agent output into a gate.
