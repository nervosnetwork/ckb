# Tx-Pool Current Convergence Projection

Projection date: 2026-08-10 Asia/Shanghai
Branch: `zhangsoledad/txpool-pipeline`
Pre-normalization recovery base: `75ef1668b415`
Normalization content checkpoint: `0099747e9a45`
Legacy compatibility content checkpoint: `5755286c2a91`
Release-boundary content checkpoint: `96cf5d14ffc2`
Architecture-synthesis content checkpoint: `8d446716c`
Settlement/location content checkpoint: `d36a7de5d`
Resource-domain closure checkpoint: `89b916447`
State: `Construction`
Completed phases: `basis_and_roadmap_normalization`,
`release_boundary_adjudication`, `architecture_optimality_synthesis`
Active phase: `registered_semantic_root_closure`
Remote policy: no push is authorized

This is the sole human current-status projection of
`security-regression-manifest.json#convergence_status`. The fixed route is
`.independent-execution-plan`. Product truth is not owned here.

## Canonical final goal and claim

The sole machine authority is
[`architecture-contract.json#/optimization_goal`](architecture-contract.json#/optimization_goal).
The target is the `X3` witness selected by hard feasibility, global static
minimum, noise-gated empirical strength on the declared matrix, then minimum
implementation/proof complexity. Independent work is maximally parallel and
coupled facts use only the unique authority's minimum atomic commit cut.

Current witness: `bounded_semantic_exchange`
Current claim: `unproved`
Certificate: absent, as required while construction is open
Canonical goal SHA-256:
`37e72f5cafb51e3a8a62664107d15e4aefc8959abe5e8c0911c7ee824f36daf0`

## Recovery integrity

The entire pre-normalization plan, progress ledger and paused R5 worktree are
preserved in local commit `75ef1668b415`. Historical document identities are:

| Artifact | Git blob | SHA-256 |
|---|---|---|
| old plan | `11a452642985c317bf2b5fde5067b7100cf2a1c9` | `35d5027a3f041e2f8448f12c020a5f417bb8715e5e62c28c5868bb4058b9ea92` |
| old progress | `90a61b8d17577c862c61f8226035aab79a40b43d` | `c7d8bd92e69a8fa37b90173242765a39cac9ca2b85165211630b4856bc0abf48` |

Root `AGENTS.md` is intentionally ignored by Git and must be copied separately
for device recovery. Current normalized identity:

```text
lines = 421
bytes = 19815
sha256 = f98a4e602f0c3284ce62847ef1635d630a46e8f1d49b0d85b89b4cd1442ecd78
```

These values must be recomputed after any further method edit and before the
next checkpoint. Conversation history is not recovery authority.

## Current device evidence tools

The repository toolchain is Rust 1.95.0 / LLVM 22.1.2 on
`aarch64-apple-darwin`. Currently available local evidence tools include
Nextest 0.9.143, cargo-mutants 27.1.0, cargo-llvm-cov 0.8.7,
cargo-semver-checks 0.50.0, Kani 0.67.0, Samply 0.13.1,
cargo-show-asm (`cargo asm`) 0.2.62 and cargo-instruments 0.4.17. TLC 2.19 is
exercised through `check_formal_models.py`.

Full Xcode 26.6 build 17F113 is installed. The system-selected directory still
points to CommandLineTools, so Instruments commands bind Xcode explicitly:

```text
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer
```

With that binding, `xctrace version` reports 16.0 (17F113) and
`cargo instruments --list-templates` succeeds with Allocations, CPU Counters,
Leaks, Power Profiler, Processor Trace, SwiftUI and Tailspin. Instruments is
therefore usable; no global `xcode-select` mutation is required.

## Actual construction rank

The ordered rank is
`rho_C = (12, 1, 1, 1, 4)`. Every listed item has exactly one blocker owner.
This is strictly below the prior `(12, 1, 3, 1, 4)` rank because the complete
resource-configuration and resource-ledger families are closed.

### `open_release_laws` (12)

- `D1-READY-OCC-LIVENESS`
- `D1-DIRECT-NEGATIVE-OCC`
- `D1-FAULT-REACHABILITY`
- `D1-FALLIBLE-SCRATCH`
- `D1-REQUEST-PAYLOAD-BOUND`
- `D1-CLOCK-DOMAIN`
- `D1-ALLOCATION-LIVENESS`
- `D1-STOP-REACHABILITY`
- `D1-TASK-CAPABILITY`
- `D1-DEPENDENCY-CLOSURE`
- `D1-FAULT-INGRESS-EVIDENCE`
- `D1-HISTORY`

### `open_compatibility_and_landing_decisions` (1)

- `landing_topology`

### `unclosed_registered_semantic_families` (1)

- `scheduler`

### `unowned_or_duplicated_production_items` (1)

- `generated_source_role_necessity_census`

### `incomplete_refinement_edges` (4)

- `conditional_complexity_lower_bound_per_dimension`
- `witness_complexity_cost_equals_lower_bounds`
- `production_refinement_and_cost_binding`
- `independent_negative_certificate_canaries`

## Unique blocker projection

| Blocker | Owned current obligations / later gate |
|---|---|
| `REGISTERED-RELEASE-LAW-CLOSURE` | all 12 `D1-*` items, exactly partitioned into 8 construction root families |
| `INDEPENDENT-EXECUTION-CUTOVER` | scheduler semantic family |
| `GLOBAL-OPTIMALITY-AND-SIMPLIFICATION` | the remaining 4 hash-bound optimality certificate requirements plus `generated_source_role_necessity_census` |
| `DEVELOP-LANDING-REHEARSAL` | `landing_topology`, including the major-version relation, migration notes and generated downstream migration evidence |
| `PERFORMANCE-ACCEPTANCE` | later frozen-binary empirical confirmation |
| `EVIDENCE-PORTABILITY` | later cold-checkout reproduction |

The manifest is the exact owner of blocker reasons and obligation lists. This
table is a projection and must be regenerated if the manifest changes.

## Completed and preserved work

Basis/roadmap normalization is complete: all current documents bind the
canonical goal, the old 12,120-line mixed history is content-addressed, the live
plan/status are bounded, and all 12 recovered laws have exactly one of 8 root
families with negative coverage/duplication canaries. This does not erase
finished product work. The checkpoint and prior commits preserve the UAK cutover,
bounded semantic exchange I1-I6, model/refinement evidence, historical mutation
cohorts and complete affected test runs. They become current evidence only when
rebound to the final frozen universe.

Release-boundary adjudication is complete. Legacy configuration and persistence
remain forward-compatible under the proved policy. For the complete public Rust
API delta, the total decision is the constant function
`compat_rust(delta) = intentional_major`; a delta generator is optional and no
facade may restore mutable transaction or policy authority. The landing
candidate domain, generated downstream-universe operators, feasibility
constraints and lexicographic cost objective are machine-owned; the actual
`L1` choice remains open until the final `X3` tree exists. The acceptance
universe now also binds the optimization certificate, landing target/rehearsal
and declared workload/environment matrix.

Architecture synthesis has content-addressed the release basis and declared
matrix, then classified all 127 generated basis atoms under the eight semantic
normal-form axes. The checker compiles the same Rust model used by Nextest: its
Cartesian quotient contains 36,864 unique forms, partitions 64 into `X0`, and
rejects the other 36,800 through explicit hard/concurrency/coupling gaps. The
extra-cost frontier has one zero-extra-cost form: the selected topology. Cost
is explicitly normalized over the release-law-implied floor shared by all
`X0` forms; its
conditional lower bound is zero in each ordered coordinate and the witness
attains it. Absolute production operation/resource bounds remain separately
owned by `production_refinement_and_cost_binding` and were not inferred from
the normalized result.

Because the executable frontier proves `|X1| = 1`, the empirical argmin over
`X1` is that same member for every objective. The generated artifact therefore
binds `X2 = X1`, the declared matrix and its typed record protocol while proving
that both the Construction measurement universe and record ledger are empty.
Fixed-binary matrix measurement remains mandatory in Acceptance and cannot
repair or select topology. Architecture synthesis is complete on this scoped
release basis; any new semantic family or legal counterexample reopens it.

The phase-boundary anti-snowball audit found no production source, authority,
state, lock, task, queue, public API or compatibility addition. In canonical
complexity-objective order, this slice has
`Delta Kappa = (0, 0, 0, 1, 0, 1, 0)`: one finite executable normal-form
algebra and one named residual certificate checker, with no new source role or
manual evidence authority. Deleting either admits an uncovered normal form or
an unverified optimality claim. Absolute `Kappa` remains owned by the later
generated source-role/necessity census; raw line count is not substituted for
that census.

Content identities at this boundary are:

- release basis: file SHA-256
  `015b12203bf636ace05b01db8ced2a272863624954568578e45ad56ecad85567`,
  canonical basis
  `ce858991458ec37250b7af47a1509715d8bd1c41ae59d9fbd541fbf3a4a64daf`;
- workload matrix: file SHA-256
  `e9167856427ff7f011a311693c542ee0f8b8448aaee884f381891c587c6b6b1d`,
  canonical matrix
  `0cd21992ba066d5fd77eba0fb91652f0465ecf0a1277e92b564f22c0837934ce`;
- normal-form/X1/X2 frontier: file SHA-256
  `7b01cf960f5efad30170e689b1ae7ca959df1f9b727beb4bbb8141bef5ae9f8c`,
  generated evidence
  `372f8714a2058c8958a80614dc37b15618e07d4dbaa928efadd6e526cd2bdda2`.

The phase review falsified the first smaller quotient: a separately bounded,
fully charged resource partition is feasible though statically dominated, and
a pure non-authoritative compatibility facade is feasible though it transfers
one additional supported public boundary and is statically dominated on the
first risk coordinate. They are explicit representatives rather than being
conflated with uncharged resources or authority-restoring facades.

The post-restart direction review was adjudicated against current state. The
certificate remains release-gating with no silent `best known` degradation;
the proposed root-closure parallel exception was rejected because the current
families overlap live synthesis axes; and Construction/Acceptance measurements
now have disjoint machine-checked record contracts. Current one-shot workloads
are deterministic from scenario parameters and runner source, while future
randomized/generated inputs must also bind their fixture/generator and seed.

The B3-R5 settlement/location family is closed:

- origin identity/view duplication was removed through a sealed producer;
- payload policy has a finite pointwise model;
- scheduler permit/ticket premises are modeled and checked;
- a legal location refresh exposed stale Accepted payload `TransactionInfo`
  while context reported pool origin;
- the root design is one payload/context location relation and one sealed
  production rebind, with no new authority, lock, task, queue or compatibility
  surface.

The apparent missing-production gate was a harness false negative: its raw
substring order matched `context` inside the guard before the struct field.
The checker now parses the exact `Some(Self { ... })` body and proves one
payload/context rebind plus one production caller. The only test-only
`with_context` helper moved from production `state.rs` into its existing child
test-support module rather than becoming an allowlisted seam. The pointwise
model and the real pool-origin refresh/refinement test are both owned by
`TP-IDENTITY-001`; the generated guide and inventory agree.

Settlement/location Nextest run
`38bcebca-0dc0-47ce-9e13-1376634e8701` passed all 14 settlement model,
refinement and location cases. Production-contract, model-refinement,
test-layout and security-manifest gates pass. The historical mutation evidence
is intentionally not regenerated: its one stale model hash and three
zero-current-match selectors remain Construction diagnostics until every root
family closes.

The resource domain is also closed without a product change. One finite model
defines the legal configuration hierarchy, continuous charge vectors, sparse
ledger, batch set transition and exact compute release. Three production
refinement tests exhaust the configured-premise masks, partial reservation and
attribution states, two-key charge sets, both caller orders and release pairs.
The production checker independently requires old-set removal before new-set
addition, checked change-local scratch bounds and no full-ledger recount.
Resource Nextest run `b5b925cb-8878-4bb3-a94d-5865f0799c8b` passed 16/16.
Production construction is limited to the runtime configuration compiler, the
authority's sole ledger and owner-derived sealed charge records. This audit
adds no state, mechanism, source role or documentation authority, so its
`Delta Kappa` is the zero vector.

The selected topology remains frozen for `registered_semantic_root_closure`.
A new semantic family, authority, topology or compatibility boundary reopens
synthesis first. Mutation regeneration, downstream performance acceptance and
remote push remain forbidden at this phase.

## Current validation facts

Current direct goal, optimality, landing, convergence, release-status, rank/
artifact binding and negative-canary validators pass. The document
goal-completeness gate, review guide, ASCII, develop refinement and
model/production frontier checks pass. TLC closes the registered
1,098-generated/318-distinct-state positive model, and its negative
configuration reproduces the required `NoEffectBlockedDirectHandoff` witness.
The aggregate security, production-contract and test-layout checkers pass. The
mutation gates retain only the declared one stale evidence hash and three
historical zero-match selectors. Focused global-normal-form Nextest run
`70f78cff-010c-485c-9740-1acc1d69473b` passed 3/3. This is a diagnostic
checkpoint, not release evidence.

Release-boundary compatibility evidence is current:

- official `origin/develop` is `17d7db5bb423a1b2177e14a132a41d5a91a515f3`;
- `ckb-app-config` Nextest run
  `90b6991a-6758-4665-9760-8f781321a8e0` passed 13/13;
- persistence Nextest run `d0448f02-e199-40ff-bd22-fa6a37fac232` passed
  all 6 `persisted::tests` cases, including legacy v1 input;
- `TP-BUDGET-001` owns missing-new-field defaults, legacy aggregate-budget
  preservation and explicit-current precedence;
- `TP-PERSIST-001` owns legacy v1 acceptance/revalidation and v2-only writes;
- node/database downgrade and reverse persistence migration remain unsupported.

The interrupted semver diagnostic remains non-authoritative history. Its default
feature heuristic enabled mutually exclusive `portable` and `march-native`
features, and the corrected run was stopped by user direction. No rerun is
required: the machine contract maps every generated public Rust API delta to
`intentional_major`. The still-open `landing_topology` obligation owns the
major-version relation, migration notes, generated workspace reverse-dependency
build/tests and rehearsal against current official `develop`.

## Exact next action

Execute `registered_semantic_root_closure` from the frozen witness by auditing
the last registered family, `scheduler`: prove the stored set, owner ring,
capability filter, overlay union, finite probe bound, cursor successor and
batch transition against every production producer. Any newly exposed semantic
case changes the model and reopens architecture synthesis before production
continues.
